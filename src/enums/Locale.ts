export enum Locale {
    zh = "zh",
    en = "en",
}

export enum DarkThemeType {
    dark = "dark",
    light = "",
}

export enum Page {
    about = "about",
    skill = "skill",
    history = "history",
    work = "work",
    dream = "dream",
}